package org.example.enums;

/**Используем класс enum(перечеслений) для работы со статусами заказа, так как это фиксированная группа значений**/
public enum Status {
    COMPLETED,
    CANCELED,
    CREATED,
    DELIVERY
}
